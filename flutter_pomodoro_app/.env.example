# Example env for Scripture API
 # Put your real API key in a local `.env` (do NOT commit)
SCRIPTURE_API_KEY=your_api_key_here

# Bible to query on api.scripture.api.bible
# Find your allowed IDs via their /bibles endpoint in their docs/dashboard.
# Example (ESV, if your key permits): 32664dc3288a28df-01
# If not set, the app defaults to '32664dc3288a28df-01' which may return 403 for many keys.
BIBLE_ID=32664dc3288a28df-01
